<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>customers detail</title>
    <link rel="stylesheet" href="styles/base.css">
    <link rel="stylesheet" href="styles/common.css">
    <link rel="stylesheet" href="styles/admin.css">
    <link rel="stylesheet" href="styles/themes.css">
  </head>
  <body>
    <div class="header">
      <ul>
          <li><a href="logout">Logout</a></li>
          <li><a href="adminhomepage">Admin Homepage</a></li>
          <li><a href="addnewadmin">Add New Admin</a></li>
          <li><a href="viewcustomersdetail">View Customer Details</a></li>
          <li><a href="updatetiers">Update Available Tiers</a></li>
      </ul>
    </div>
    <div class="main">
        <div class="h12"></div>
        <div class="content">
          <div class="h40"></div>
          <div class="top">
          <div class = content_title>Customers Detail</div>


          <form action="///////.rb" target="_blank" method="get">
          </form>
              
             <table>
              <tr>
                <th>Twitter ID</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Twitter Handle</th>
                <th>Email</th>
              </tr>
                
             <% @results.each do |element| %>
               <tr>
                 <td><%=h element[0]%> </td>
                 <td><%=h element[1]%> </td>
                 <td><%=h element[2]%> </td>
                 <td><%=h element[3]%> </td>  
                 <td><%=h element[4]%> </td>        
               </tr>
             <% end %>  
             </table>   
              

            <!-- PRIMARY KEY(order_id),
  FOREIGN KEY(tier_id) REFERENCES CarTiers(tier_id),
  FOREIGN KEY(user_id) REFERENCES UserInfo(user_id) -->



          
            <div class="h12"></div>
          </div>
        </div>
      </div>
  </body>
</html>